# R22 UI、主题、背景与国际化 Review

审查日期：2026-09-12。审查基线为 `8ea9cf4377cbec642e57ec3b9b3498a7ae13d893` 加本次工作区差异。状态：背景配置、图片库、主题入口和生命周期回归已完成审查；本批确认的 background object URL 与删除当前图片问题已修复并通过。真实安装版视觉验收、六语言逐页审阅和辅助功能设备测试未执行。

## 调用链与契约

- `D:/Programs/Kokoro-Engine/src/ui/hooks/useBackgroundSlideshow.ts:16-45` 定义默认背景配置、localStorage 持久化和无图片时的模式归一化。
- `D:/Programs/Kokoro-Engine/src/ui/hooks/useBackgroundSlideshow.ts:48-121` 从 IndexedDB 加载图片并创建 object URL；当前保存 `storedImagesRef`，组件卸载时释放仍存活的 URL；数据库加载晚到时由 `active` guard 阻止状态提交。
- `D:/Programs/Kokoro-Engine/src/ui/hooks/useBackgroundSlideshow.ts:135-242` 处理导入、删除、清空和 URL/当前索引同步；删除当前图片后选择剩余有效图片，数据库失败时不提前修改 UI。
- `D:/Programs/Kokoro-Engine/src/ui/hooks/useBackgroundSlideshow.ts:244-310` 实现 sequential/random 轮播、定时器和无图 fallback。
- `D:/Programs/Kokoro-Engine/src/ui/widgets/BackgroundLayer.tsx:11-55` 负责背景图 crossfade、blur 和可读性遮罩；`D:/Programs/Kokoro-Engine/src/ui/widgets/settings/BackgroundTab.tsx:20-102` 负责导入、删除、二次确认清空和反馈计时器。
- 主题和语言由 `D:/Programs/Kokoro-Engine/src/ui/theme/ThemeContext.tsx`、`D:/Programs/Kokoro-Engine/src/ui/i18n.ts` 及 `D:/Programs/Kokoro-Engine/src/ui/locales/*.json` 共同消费；本轮未修改这些生产入口。

## 必测场景结果

| 场景 | 结果与证据 |
| --- | --- |
| 默认 static/slideshow/generated 模式归一化 | 自动通过：`useBackgroundSlideshow.test.ts` 与 settings groups/background asset 测试共 13 项通过。 |
| 删除当前图片后选择有效剩余图片 | **修复后自动通过**：`useBackgroundSlideshow.lifecycle.test.tsx` 验证撤销旧 URL、保留 `blob:image-2` 并把它设为当前 URL；修复前失败证据见 [R20-R22-lifecycle-red.txt](evidence/R20-R22-lifecycle-red.txt)。 |
| hook 卸载时释放全部 object URL | **修复后自动通过**：生命周期测试验证两个 URL 均调用 `URL.revokeObjectURL`。 |
| 清空图片、删除失败和反馈错误 | 自动通过：背景设置相关现有测试覆盖 DB 删除/清空错误反馈；当前 hook 清空成功后同步清空 refs 和当前 URL。 |
| 轮播 timer、random/sequential 和空库 fallback | 自动通过/静态通过：现有 hook 测试覆盖配置归一化，定时器 cleanup 位于 effect 返回值；真实 30 分钟轮播和浏览器 tab 后台恢复未执行。 |
| 主题、长文本、焦点和六语言完整界面 | 静态检查入口与 locale 文件；未逐语言逐页面执行键盘/屏幕阅读器/安装版视觉验收。 |

## 本批修复证据

- `useBackgroundSlideshow.ts` 通过 `storedImagesRef` 保持当前 object URL 集合，并在 unmount cleanup 中逐个 revoke；导入、删除和清空路径同步维护该 ref，避免卸载遗漏或重复依赖旧 closure。
- 删除成功后按当前 URL 和索引计算剩余图的当前项；最后一张图片删除会清空当前 URL 并归一化 slideshow 配置。
- 定向命令：`npx.cmd vitest run src/ui/hooks/useBackgroundSlideshow.lifecycle.test.tsx src/ui/hooks/useBackgroundSlideshow.test.ts src/ui/widgets/settings/settings-groups.test.ts src/ui/widgets/background-asset-url.test.ts`，4 个文件、13 项通过。

## 限制与剩余风险

- 未在 WebView2/安装版验证 object URL 在长时间轮播、页面隐藏/恢复、背景切换和热重载中的实际资源占用。
- 未验证真实 IndexedDB 迁移、磁盘满、权限错误、损坏 Blob 和大尺寸图片解码；相关文件测试使用 mock DB。
- 未完成六语言缺键扫描、键盘焦点遍历、屏幕阅读器和极端缩放验收；这些属于 R22 剩余验证，不因 hook 测试通过而关闭。
