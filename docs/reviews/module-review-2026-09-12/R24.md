# R24 开发脚本、Git Hooks 与文档

状态：脚本、CI和文档静态审查，现有Vitest脚本回归已随753项基线通过。未运行真实目录清理。

## 审查范围

- prune-target.mjs：canonical路径/reparse校验、active compiler与文件锁、deps分类、冷却与branch marker、dry-run、hook保留和幂等安装；对应prune-target.test.mjs。
- build-content-registry.mjs与manifest/schema一致性归R16。
- check-ipc-contract.mjs只检查静态字符串命令是否注册；动态命令、payload及event仍需域审查。
- .github/workflows/ci.yml和三平台build脚本，.githooks与validate-sherpa-onnx-cache.sh。
- CONTEXT、ADRs、architecture、releasing、资产许可表与实际代码/资源。

## 确认问题

### R24-001 / P2：默认测试发现旧工作树

vite.config.ts没有限定测试include；npm test发现.worktrees/pr20-audio-lifecycle，旧代码测试解析当前@ alias，导致143文件中16失败。当前src/scripts排除工作树后72文件753项全部通过。本轮已经保存这两个真实结果。修复增加明确Vitest项目include，不删除工作树、不掩盖当前源码失败。

### R24-002 / P3：文档架构与现状漂移

architecture.md仍标2026-04-09，包含已不存在的FooterBar/TelegramTab和旧语言数量；实际BotTab及zh-TW等需同步。releasing.md描述build workflow仅在tag运行，实际还包括main push和手动触发，发布步骤自身仍受tag条件保护。修复仅更新确认的布局、语言和触发说明，不把旧release evidence改写为当前通过。

## 必测结果及限制

| 场景 | 证据 |
| --- | --- |
| dry-run不写、活动构建保护、路径逃逸、hook幂等与保留 | baseline-frontend-scoped.log中脚本用例通过；作用于各自临时fixture。 |
| POSIX Git hook权限和执行 | 基线远程CI Frontend20/22任务中的Exercise Git Sentinel hooks成功，见github-ci-jobs.log。 |
| NSIS升级/卸载保留 | 静态hook核对，实际安装阻塞见R23。 |
| 资产许可 | 内置Hiyori有notice；许可审计中Haru未明确可再分发，仍保留限制，不自动宣称许可完成。 |
| CI缓存清理 | shell脚本按CARGO_TARGET_DIR执行递归删除，当前CI未给外部target值；脚本独立运行缺少canonical guard，列维护风险，未执行。 |
| 文档示例 | 对核心npm/Cargo命令已有实际结果；涉及真实provider/渠道/外部发布的示例未联调。 |

默认测试修复与架构文档更新进入修复批次。新增测试必须验证生产行为，不以测试体复制一份流程来证明正确性。

## 修复复核（2026-09-12）

Vitest 配置已排除 `.worktrees`，默认测试 78 files/766 tests 通过；脚本定向 137 tests 通过；架构文档漂移仍保留为 P3 待办。
