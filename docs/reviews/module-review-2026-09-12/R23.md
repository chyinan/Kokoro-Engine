# R23 构建、原生依赖与安装

## 本轮证据

- 前端构建、Rust 874测试、Clippy通过，见BASELINE.md。
- 当前基线HEAD的远程CI与三平台打包任务均success，查询已保存evidence/github-baseline-ci.log；CI具体job保存github-ci-jobs.log。它们只证明基线HEAD，不能代表本轮未提交修复。
- CI运行：https://github.com/chyinan/Kokoro-Engine/actions/runs/34604932181 。macOS打包34604932251、Linux34604932271、Windows34604932317。
- 本机Windows release应用编译成功，但NSIS工具下载阶段失败：校验发现缓存hash不符并自动重下官方nsis_tauri_utils.dll，随后timeout: global。未绕过完整性校验；evidence/baseline-windows-installer.log。

## 静态审查

检查Tauri build/resources、Cargo profile、build.rs动态库下载/寻找/复制、运行时ORT路径选择、NSIS uninstall hook、三平台workflow、Sherpa cache guard与Nix。

- app前端、Cargo与Tauri版本当前0.4.0；flake.nix:25仍硬编码0.2.9，包装元数据失配（R23-001/P2）。修复改为读取仓库package版本，避免后续再次漂移。
- build.rs只以存在且非空判断动态库可用；真实ABI验证在运行时。前述Rust测试实际可运行，证明本机测试环境能够加载所需native依赖，不能推断安装后分发文件齐全。
- NSIS hook强制不删除app data，符合升级/卸载保留数据契约。未在用户真实配置上运行安装/升级。
- dynamic-load的ORT是否进入最终bundle需要检查最终NSIS payload，当前打包失败，尚未确认这一项。
- release编译有debug条件分支下的unused变量警告，未导致build失败；与--lib dev Clippy通过分别记录。

## 必测状态

| 项目 | 状态 |
| --- | --- |
| Windows release编译 | 通过；当前NSIS打包受官方工具下载超时阻塞。 |
| macOS/Linux基线CI编译与安装包 | HEAD匹配的远程记录success；本轮修复尚未上CI。 |
| 新安装、升级保留数据、中文/空格路径 | 未执行：缺少隔离用户运行环境，当前包构建也未完成。 |
| 安装后native库/Live2D/MOD资源 | 待最终installer payload和隔离桌面运行验证。 |
| Nix | 静态审查发现版本漂移；本机无Nix运行证据。 |

## 环境限制

未找到WindowsSandbox.exe。源代码硬编码dirs_next用户数据路径，仅修改Tauri identifier或环境APPDATA不足以证明隔离。已有旧0.2.x installer不作为0.4.0证据。不会用真实用户数据补足隔离桌面验收。

## 修复复核（2026-09-12）

flake.nix 已对齐 0.4.0。Windows release 可执行文件已编译，但 NSIS 官方工具下载因 hash 不符重下后超时，未生成可安装包；安装/升级验收仍阻塞。
