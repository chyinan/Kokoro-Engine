# R20 Live2D 与交互 Review

审查日期：2026-09-12。审查基线为 `8ea9cf4377cbec642e57ec3b9b3498a7ae13d893` 加本次工作区差异。状态：Live2D URL 映射、controller 订阅和 React 生命周期已完成静态审查；本批确认的两个前端生命周期回归已修复并通过。真实 Cubism/WebGL、损坏模型和安装版资源加载未执行。

## 调用链与契约

- `D:/Programs/Kokoro-Engine/src/features/live2d/Live2DViewerLoader.tsx:10-81` 延迟加载 Cubism core 和 `Live2DViewer`，初始化失败时展示错误；组件卸载后不再提交异步加载结果。
- `D:/Programs/Kokoro-Engine/src/features/live2d/live2d-model-url.ts:1-80` 将内置/用户库 ID 映射到 `live2d://`，将已验证的包内绝对路径映射到 `asset://`。
- `D:/Programs/Kokoro-Engine/src/features/live2d/Live2DViewer.tsx:93-198` 创建/销毁内部 `Live2DController`，加载 profile 更新订阅；`:228-283` 订阅 chat cue 与 idle behavior；`:318-780` 创建 PIXI app、加载模型、处理鼠标/指针交互、ticker、resize 和资源销毁。
- `D:/Programs/Kokoro-Engine/src/features/live2d/Live2DController.ts:35-68` 把 audio amplitude 转为 lip-sync，并在构造时注册 audio listener；`:383-385` 通过 `destroy()` 移除 listener。

## 必测场景结果

| 场景 | 结果与证据 |
| --- | --- |
| 包内绝对路径和库 ID 映射 | 自动通过：`live2d-model-url.test.ts` 2 项通过；包路径不再落到库协议。 |
| Live2D profile/chat cue/idle listener 延迟注册后卸载 | **修复后自动通过**：`Live2DViewer.lifecycle.test.tsx` 的 delayed registration 用例通过；每个 Promise 在卸载后完成时立即调用 cleanup。修复前失败证据见 [R20-R22-lifecycle-red.txt](evidence/R20-R22-lifecycle-red.txt)。 |
| React StrictMode effect remount 的 audio listener | **修复后自动通过**：StrictMode 用例通过，`addAmplitudeListener` 与 `removeAmplitudeListener` 净差为 1；内部 controller cleanup 后清空 ref，第二次 effect 创建新的 controller。 |
| 模型加载取消、ticker、pointer 和窗口可见性清理 | 静态通过：`cancelled` guard、ticker/remove、visibility listener 和 PIXI destroy 均有 cleanup；真实异步 Cubism 加载和 WebGL context 未执行。 |
| cue、motion、expression、lip-sync 和 hit-area 交互 | 静态检查通过：事件交由 controller，pointer gesture 通过 interaction service；真实模型资源和真实音频 amplitude 未联调。 |

## 本批修复证据

- `Live2DViewer.tsx` 的三个订阅 effect 都维护 `disposed`；异步注册完成后若组件已卸载立即调用返回的 unlisten，否则保存给 cleanup。
- 内部 controller 的 effect cleanup 在 `destroy()` 后设置 `internalControllerRef.current = null`，避免 StrictMode 第二次 effect 误以为旧 controller 仍有效。
- 定向命令：`npx.cmd vitest run src/features/live2d/Live2DViewer.lifecycle.test.tsx src/features/live2d/live2d-model-url.test.ts`，2 个文件、4 项通过。

## 限制与剩余风险

- 未启动真实 PIXI/Cubism 模型，未验证模型 JSON 引用缺失、纹理解码失败、GPU context loss、连续模型切换和显存释放。
- 未在安装版验证 `asset://` 包资源、含空格/非 ASCII 路径、DPI 和多屏布局；R21 负责辅助窗口联合验收。
- `loadProfileForModel` 的后台 profile 读取虽由 controller request ID 防止旧 profile 覆盖新请求，但没有真实慢 IPC 与组件销毁联调证据。
