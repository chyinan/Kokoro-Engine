# R16 Registry、内容库与内容构建

状态：已完成本轮静态审查及现有离线回归；真实远程源安装和安装后重启待独立桌面环境。

## 审查链路

- commands/registry.rs:83–200：HTTPS、无用户密码、禁重定向、连接/请求超时，index 4 MiB/archive 64 MiB，流式累积在扩容前限额。
- commands/registry.rs:234–398：下载暂存独占创建、重读后重验 hash/manifest，官方来源+identity+准确包路径绑定；URL 安装固定 community。
- registry/client.rs:78–253 与 characters/catalog.rs:308–765：schema/manifest/engine/checksum；大小写折叠重复项、重复分隔符、文件清单、staging 和推广/回滚。
- commands/registry.rs:447–574：移除前确定当前实例与模板版本关系，暂存移除，重新激活同一实例的内置展示 fallback，失败恢复。
- ContentLibrary/content-library-state：来源确认、MOD 权限展示、失败重试与 authoritative state 重载；安装/移除后目录同步。
- scripts/build-content-registry.mjs 及其 Vitest：构建端与安装端文件、manifest、语义版本、体积及输出目录策略一致性。

## 必测结果

| 场景 | 本轮证据 |
| --- | --- |
| 自定义源/URL 不获得官方标签 | registry_tests 的 source/trust/identity/package origin 用例及 ContentLibrary 测试：包含于 baseline-rust 874、frontend-scoped 753 全量通过。 |
| checksum、畸形归档、版本与下载大小 | commands::registry_tests 中相应用例通过；检查实际 fetch_bytes 与 read_response_bytes 接线。 |
| traversal/case collision/repeated separator/不支持脚本 | 安装、catalog 与 builder 现有测试通过。 |
| symlink/junction/输出目录逃逸 | catalog 与 builder 现有临时目录回归通过；没有对真实用户目录运行。 |
| 更新失败和活动包移除 | staging/fallback/rollback 回归通过；完整 Tauri UI→重启链未执行。 |
| 重复构建、许可文件和 template 排除 | builder 合约测试通过；未重写或发布 repository registry 产物。逐字节双构建独立验收仍未运行。 |

## 发现与限制

本轮未确认本包新增产品缺陷。可执行 MOD 的权限执行问题归 R15；备份使用 registry 解析的后续恢复问题归 R17，避免重复计数。

本地合约回归不等于真实远程索引、归档或发布渠道已可用。实际网络下载、安装后进程重启和真实资源渲染仍列未验证。

## 修复复核（2026-09-12）

Registry 与内容库本轮没有需要修改的产品缺陷；builder/installer/trust 测试继续通过。真实远程源、发布归档和安装后重启保持未验证。
